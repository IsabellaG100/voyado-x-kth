# GitHub Personal Access Token for API access during the workshop.
# Create one at https://github.com/settings/tokens with "repo" scope (read-only is fine).
# Copy this file to .env.local and fill in your token.
VITE_GITHUB_TOKEN=
